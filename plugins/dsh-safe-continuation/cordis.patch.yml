# The bundle inserts the host loader row. Its default is intentionally disabled;
# enable it per profile after reviewing the bounded continuation budgets and
# guard policy documented in README.md.

- insert:
    - id: safe-continuation
      name: dsh-safe-continuation
      config:
        enabled: false
